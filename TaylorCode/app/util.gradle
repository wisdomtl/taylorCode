

//custom function in separate gradle file
ext.getVersionFromCmd = { ->
    return project.findProperty("version")
}

// case: define val and read it in separate file
gradle.ext.taylorVal = "taylor"
